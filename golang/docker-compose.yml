version: "3.9"

services:
  bot:
    build: .
    image: study-bot:latest
    container_name: study-bot
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./files:/files
    network_mode: "host"
